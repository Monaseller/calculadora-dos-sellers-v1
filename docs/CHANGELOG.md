# CHANGELOG.md — Calculadora dos Sellers (CDS)

> Ordem cronológica reversa. Toda tarefa que criar, corrigir ou remover funcionalidade deve adicionar uma entrada aqui antes de finalizar.

## 2026-07-11

- **Fase 5 Etapa 2 concluída:** `lib/sync-ml.ts` (`syncMLForUserV2`) passa a gravar `loja_id` em pedidos novos, resolvido pelo identificador estável `seller_id` (via `/users/me`), nunca por `access_token`. Fluxo por cookie (legado) e fluxo por banco (`getMLLojaAtiva`) ambos resolvem e validam `loja_id` antes de gravar; ausência de correspondência única interrompe o sync (nenhum upsert parcial). Testado nos dois fluxos, `tsc` limpo, `loja_id` populado confirmado por SQL.
- **Redesenho do botão "Sincronizar" da tela Vendas — implementado, aguardando testes manuais e execução da migration.** Motivo: o botão disparava sync bloqueante dentro da própria rota HTTP de leitura (`sync=1` em `/api/ml/vendas`/`/api/shopee/vendas`); um timeout/erro em um marketplace zerava o array combinado inteiro, derrubando o faturamento exibido mesmo com o outro marketplace saudável. Nova arquitetura:
  - Tabela `sync_jobs` (`supabase/migrations/20260711_sync_jobs.sql`, **NÃO EXECUTADA AINDA**) — fila persistente por `loja_id`, com tipo `incremental`/`backfill`, índice único parcial (lock por loja), `heartbeat_em` (substitui timeout fixo por heartbeat, aprovado: 10min sem heartbeat = abandonado), `tentativas`/`max_tentativas` (retry só para erro transitório; `LojaIdIntegrityError` é permanente, sem retry), função `claim_next_sync_job()` com `FOR UPDATE SKIP LOCKED` (aquisição atômica, evita 2 workers pegando o mesmo job).
  - `LojaIdIntegrityError` extraída para `lib/sync-errors.ts` (antes era classe local dentro de `syncMLForUserV2`) — necessário para `app/api/internal/sync/executar` classificar erro permanente vs. transitório via `instanceof`.
  - `lib/sync-ml.ts`/`lib/sync-shopee.ts` (via novo parâmetro `lojaOverride` em `syncMLForUserV2`, já existente em `syncShopeeForUserV2`) e novas `getShopeeLojaById`/`getMLLojaById` (`lib/shopee-auth.ts`/`lib/ml-auth.ts`) permitem sincronizar uma loja específica por id, não mais só "a mais recente ativa" — necessário para o worker atender jobs de uma loja exata.
  - `app/api/internal/sync/executar/route.ts` (nova, protegida por segredo estático `SYNC_WORKER_INTERNAL_SECRET`, nunca pelo cookie de sessão): executa o sync de fato, chamando `syncShopeeForUserV2`/`syncMLForUserV2` em processo.
  - `app/api/sync/iniciar/route.ts` (nova): `POST { loja_id }` — servidor calcula `date_from=ontem`/`date_to=hoje` (nunca aceito do cliente), resolve `marketplace` do banco, cria job `incremental` ou retorna o job já ativo.
  - `app/api/sync/status/route.ts` (nova): `GET ?job_id=` (uso primário, polling) ou `?loja_id=` (exibição passiva).
  - `scripts/sync-worker.mjs` (novo): processo Node separado (sem `tsx`/`ts-node` — chama a rota interna via `http.request`, mesmo padrão de `scripts/reconciliar-lotes.mjs`, nunca importa `.ts` diretamente), loop contínuo: reclama jobs travados, faz claim atômico, heartbeat a cada 30s, executa, aplica retry. **Precisa estar rodando manualmente (`node scripts/sync-worker.mjs`) para qualquer sincronização acontecer — solução local desta fase, processador de produção fica para depois.**
  - `app/api/ml/vendas/route.ts`/`app/api/shopee/vendas/route.ts`: removido o branch `sync=1` (sync bloqueante inline); passam a expor `lojaId` na resposta. Auto-sync de cache vazio/stale mantido inalterado (fora de escopo).
  - `app/(app)/vendas/page.tsx`: `rows` dividido em `mlRows`/`shopeeRows` independentes (nunca zerados pela falha do outro nem por falha própria — só atualizam em sucesso); disparo do botão (`dispararSincronizar`) separado da leitura (`refetchTudo`/`lerMarketplace`); novo estado `atualizando` (distinto de `loading`) e `syncMsg` (mensagem discreta em erro/timeout, dados antigos preservados); `semConexao` agora só ativa se não havia nenhum dado já exibido.
  - Escopo mantido: só Vendas. Dashboard, resumos diários, fórmulas, cron e deploy não tocados.
  - **Pendente:** usuário revisar `supabase/migrations/20260711_sync_jobs.sql` e executar manualmente; adicionar `SUPABASE_SERVICE_ROLE_KEY`/`SYNC_WORKER_INTERNAL_SECRET` em `.env.local`; rodar `npx tsc --noEmit` localmente; testar worker/status/heartbeat/lock/preservação de tela. Nenhum commit/push/deploy feito.
- **Migration `pedidos.loja_id` executada e backfill concluído.** `ALTER TABLE pedidos ADD COLUMN loja_id UUID REFERENCES lojas(id)` (`supabase/migrations/20260711_loja_id_pedidos.sql`) rodada no Supabase. Backfill (`scripts/backfill-loja-id.sql`, passo a passo, validado pelo usuário a cada etapa) casou `(user_id, marketplace, conta=nickname)` com `lojas` — **cobertura de 100%: 350.298 de 350.298 pedidos** ganharam `loja_id`, zero sem correspondência, zero colisão de nickname entre lojas (checado inclusive contra lojas desconectadas). Correção feita durante a execução: joins precisaram de cast explícito `l.user_id::text = p.user_id` (`lojas.user_id` é UUID, `pedidos.user_id` é TEXT — tipos diferentes para o mesmo dado; Supabase/PostgREST faz esse cast sozinho nas chamadas da aplicação, SQL puro não). Nenhuma regra financeira alterada — só a coluna nova foi escrita. `dashboard_resumos_diarios` (ainda não criada no banco) já nasce com `loja_id` como parte da chave, em vez de `(marketplace, conta)`. Ver `DECISIONS.md` e `ROADMAP.md` Fase 5.
- **Achado documentado, fora de escopo por decisão do usuário:** 9 registros em `lojas` com `ativo=true` e `user_id=NULL` (7 ML, 2 Shopee) — inofensivos hoje (nenhuma leitura real os alcança), causa não investigada. Ver `BUGS.md`.
- Pendente (próxima etapa, aprovação separada): `sync-shopee.ts`/`sync-ml.ts` ainda não gravam `loja_id` em pedidos novos; código de leitura (Dashboard/Vendas/resumos) ainda não usa `loja_id`.

## 2026-07-10

- **Auditoria financeira Shopee encerrada formalmente.** Removido bloco de log temporário em `lib/sync-shopee.ts` (imprimia pedido bruto completo, incluindo dados de comprador, no terminal a cada sync — marcado desde 2026-07-07 como "remover apos validacao"). Atualizados `PROJECT_CONTEXT.md`, `DECISIONS.md`, `ROADMAP.md`, `BUSINESS_RULES.md`, `DATABASE.md` com o fechamento: fonte real de dados financeiros é `get_escrow_detail` (não `get_order_detail`), `escrow_amount_after_adjustment` como regra, critério de `has_income_data` independente do sinal do escrow, reconciliação validada em >7.500 pedidos reais, fórmula de "Vendas sem os descontos da plataforma" permanece sem confirmação (não bloqueante). Nenhuma regra financeira nova implementada nesta entrada — só documentação e remoção de log. Início da etapa de arquitetura de performance (resumos diários) — ver `ROADMAP.md`.

## 2026-07-07 a 2026-07-09 — Reconciliação financeira Shopee (`get_escrow_detail`)

- Criada rotina separada `app/api/admin/shopee/reconciliar-financeiro/route.ts` (fora do sync principal): consulta `/api/v2/payment/get_escrow_detail` por `order_sn`, seleção por `status_shopee_raw='COMPLETED'` + `has_income_data=false` + `escrow_amount=0` (ou lista/`order_id` específico), dry-run por padrão, idempotente (`force=1` para reprocessar), rateio proporcional por item via `item_subtotal`. Parâmetros de transparência: `limit_solicitado/limit_aplicado/max_limit_permitido/limit_foi_reduzido`. Teto (`MAX_LIMIT`) elevado de 100 para 500. Parâmetro `order_ids` (lista) adicionado para reprocessar conjuntos pontuais. Migration de índices `20260707_indices_reconciliacao_financeira.sql` (corrigiu timeout real de Postgres no primeiro teste ao vivo).
- Confirmado ao vivo: `get_escrow_detail` funciona para contas Open Platform Brasil (não havia confirmação documental prévia).
- Corrigido critério de valor: `escrow_amount_after_adjustment` como principal (não `escrow_amount` bruto) — divergência real encontrada em pedido com débito DIFAL.
- Corrigido critério de `has_income_data`: `true` sempre que `order_income` existir, independente do sinal de `escrow_amount` (antes exigia `> 0`, o que classificava devolução total e débito pós-reembolso como "sem dado real").
- Escalado em lotes (5 → 7.500+ pedidos reais gravados), validado por SQL a cada etapa (sem gravação parcial, sem duplicata).
- Criado `scripts/reconciliar-lotes.mjs`: orquestração local de múltiplos lotes sequenciais via HTTP (usa `http` nativo do Node, não `fetch()`, para evitar o timeout padrão de 5min do undici em lotes longos), com `--batch-size`/`--max-batches`/`--dry-run`/`--force`, parada imediata em erro (stop_on_error), resumo acumulado. Adicionado `tempo_lote_ms` na resposta da rota para calibrar volume por execução (~500 pedidos ≈ 10min).
- Auditoria de campo (2.055 pedidos, cobertura 100% do período de referência do Seller Center): testadas e rejeitadas as hipóteses `seller_product_rebate.amount`, `promotion_list.platform_sale`, combinações por força bruta (descartadas como coincidência estatística) para reproduzir "Vendas sem os descontos da plataforma". Auditoria estrutural completa dos ~130 campos de `get_escrow_detail` não encontrou candidato. Fechado como limitação documentada, não bug — ver `DECISIONS.md`.
- Paginação de `app/api/shopee/vendas/route.ts` e `app/api/ml/vendas/route.ts` corrigida: `.select()` sem `.range()` era truncado silenciosamente pelo limite padrão de linhas do PostgREST (confirmado: 1000 retornadas vs. contagem real maior). Corrigido com busca em páginas de 1000 via `.range()` até página incompleta.

## 2026-07-06

- **Fase D implementada.** Seletor global "Data de Pagamento" (padrão) / "Data de Criação" no frontend, afetando automaticamente Dashboard, Vendas, KPIs, gráficos, balancete, produtos e o comparativo ML×Shopee do Dashboard. Criado `lib/date-field-context.tsx` (`DateFieldProvider`/`useDateField`), estado persistido em `localStorage` (`cds_date_field_v1`), padrão `"pagamento"`. Envolvido `app/(app)/layout.tsx` com o provider; toggle visual adicionado na `TopBar` (visível em todas as páginas de `(app)`, já que a `TopBar` é renderizada pelo layout comum). `app/(app)/dashboard/page.tsx` e `app/(app)/vendas/page.tsx` passaram a ler `dateField` do contexto e propagar `?date_field=` nas chamadas a `/api/ml/vendas` e `/api/shopee/vendas` — incluindo `dateField` nas dependências dos `useCallback`/`useEffect` responsáveis pelo fetch, para forçar refetch ao trocar o seletor (sem isso a troca ficaria "presa" mostrando dados do modo anterior). Nenhuma alteração em cache/sync/regra financeira, conforme escopo. `npx tsc --noEmit`: zero erros. Revisão manual confirmou os 6 fluxos pedidos (Hoje/Ontem/Últimos 7 dias/Este mês × Data de Pagamento/Data de Criação) disparam refetch corretamente. Detalhe de telas migradas vs. legadas em `PROJECT_CONTEXT.md`.
  - **Nota do ambiente:** durante a implementação, o sandbox usado para rodar `tsc` sofreu um problema real de sincronização de mount (não é bug de código): escritas feitas via bash para os mesmos caminhos já editados pelas ferramentas de arquivo, em alguns casos, duplicaram ou truncaram conteúdo diretamente no arquivo do projeto (não uma cópia isolada). Detectado via `git diff --stat` (contagens de linhas anômalas) e corrigido revertendo/reescrevendo os 3 arquivos afetados (`components/TopBar.tsx`, `app/(app)/dashboard/page.tsx`, `app/(app)/vendas/page.tsx`) a partir do conteúdo correto já confirmado pelas ferramentas de arquivo. Resultado final verificado linha a linha e via `tsc` limpo — mas registrado aqui porque foi um incidente real de manipulação de arquivo, não apenas uma nota de rodapé.
- **Fase C implementada.** `app/api/shopee/vendas/route.ts` e `app/api/ml/vendas/route.ts` ganharam o parâmetro `date_field=pagamento|criacao` (padrão `pagamento`). Antes de implementar, foi levantado (e resolvido com o usuário) um conflito entre a regra "só `data_pagamento IS NOT NULL` entra na visão financeira" e a regra "manter fallback legado quando a nova coluna está NULL": um fallback genérico reintroduziria a contaminação create_time/pay_time que a arquitetura de três datas existe para eliminar, porque pedidos pós-Fase-B genuinamente não pagos também têm `data_pagamento` NULL. Solução implementada: o fallback para a coluna `data` legada só se aplica quando `data_criacao` **também** é NULL (ou seja, só em pedidos nunca tocados pelo sync pós-Fase-B) — pedido novo sem pagamento nunca cai no fallback, fica de fora da visão financeira sem exceção, por decisão explícita do usuário. Query implementada via `.or()` do Supabase com `and(...)` agrupado. `data_atualizacao_marketplace` não é usada em nenhum filtro (regra 5). Frontend, cache/performance e APIs de sync não foram tocados. `npx tsc --noEmit`: zero erros. Detalhe completo em `BUSINESS_RULES.md` ("Fase C — date_field nas APIs de vendas").
- **Fase B (Mercado Livre) implementada.** `lib/sync-ml.ts`: adicionado bloco de cálculo logo após o filtro `dataPedido < dateFrom || dataPedido > dateTo` (antes de `inferLogistica`), calculando `data_criacao` (sempre a partir de `date_created`, sem shift de timezone — o ISO do ML já traz offset embutido), `data_pagamento` (só quando existe pagamento com status `approved`/`partially_refunded` e `date_approved` — `NULL` sem fallback automático quando não há pagamento, mesma regra estrita da Shopee) e `data_atualizacao_marketplace` (a partir de `order.last_updated`; **não verificado empiricamente nesta sessão** se o campo realmente vem no recurso de Order do ML — se não vier, fica `NULL` de propósito, conforme instrução do usuário). As três colunas foram adicionadas ao único `rows.push` existente (ML só tem um ponto de inserção, diferente da Shopee que tem dois). Nenhuma regra de relatório, frontend ou API de vendas foi alterada — a coluna `data`/`dataPedido` legada permanece exatamente como antes. `npx tsc --noEmit` rodado: zero erros (o ambiente do assistente teve um problema pontual de mount desatualizado do arquivo durante a verificação, resolvido reescrevendo o conteúdo já confirmado correto via Read; o resultado final do type-check é limpo). Com isso, Fase B está concluída para os dois marketplaces (Shopee + ML).
- **Fase B (Shopee) implementada.** `lib/sync-shopee.ts`: adicionado `update_time` aos campos buscados em `get_order_detail`; cada pedido agora calcula `data_criacao` (sempre a partir de `create_time`, independente de `noBuffer`), `data_pagamento` (só quando `pay_time` existe — `NULL` sem fallback automático quando o pedido nunca foi pago) e `data_atualizacao_marketplace` (timestamp bruto de `update_time`, TIMESTAMPTZ). As três colunas são gravadas nos dois pontos de `rows.push` (linha normal por item e a linha "sem item_list"). A lógica de `dataBrt`/`refTs`/coluna `data` (legada) permanece sem alteração — continua decidindo o range da busca e alimentando o fallback, exatamente como antes. `npx tsc --noEmit` rodado: zero erros. ML (Fase B parte 2) ainda não iniciado — aguardando confirmação do usuário para prosseguir.
- Aprovada decisão de arquitetura: separação de `data_criacao`/`data_pagamento`/`data_atualizacao`, reposicionando a CDS de "calculadora" para "ERP financeiro multi-marketplace". Usuário respondeu aos 5 pontos levantados (nomenclatura genérica, regra estrita de NULL para pedido não-pago em vez de fallback automático, ML sem update_time por enquanto, bug de status separado, sem backfill agressivo). Criado (não executado) `supabase/migrations/20260706_separacao_datas.sql`: adiciona `data_criacao`, `data_pagamento`, `data_atualizacao_marketplace` + índices na tabela `pedidos`, mantendo `data` como fallback legado. Atualizados `PROJECT_CONTEXT.md`, `BUSINESS_RULES.md`, `DECISIONS.md`, `ROADMAP.md`, `DATABASE.md`. Nenhum código de sync/API/frontend foi alterado ainda — só schema proposto (Fase A).
- Identificada causa exata dos 12 pedidos divergentes (6 só-Shopee, 6 só-CDS) do `verify-979`, lendo `verify-979.json` (salvo pelo usuário, evitando corrupção de copy-paste) e cruzando com `app/api/sync/route.ts` e `lib/sync-shopee.ts`. Achados: (1) cron usa janela rolante de só 2 dias por `update_time`, que já passou de 02-04/07 — explica 5 dos 6 só-Shopee; (2) cron exclui explicitamente pedidos `COMPLETED` da listagem (`filtrarCompleted`) — explica o 6º só-Shopee, e é um bug real (pedido novo que já nasce completo nunca entra); (3) 4 dos 6 só-CDS são diferença de definição `create_time` vs `pay_time` (não bug) — incluindo um caso literal de fronteira de 1-2 minutos antes/depois da meia-noite BRT, confirmando com pedido real a hipótese original do `boundary-audit`; (4) 2 dos 6 só-CDS são pedidos cancelados/estornados na Shopee cujo status ficou desatualizado (`paid`) no banco porque nada revisita pedidos antigos já sincronizados. Tabela completa entregue ao usuário no chat. Ver `BUGS.md` e `DECISIONS.md`.
- Rodado `verify-979`: conjuntos NÃO são idênticos apesar de 979=979. 6 pedidos só-Shopee, 6 só-CDS, 973 em ambos. Editado `app/api/debug/verify-979/route.ts` (mesmo endpoint, sem criar novo) para adicionar: `create_time`/`pay_time`/`update_time` de cada pedido, busca ampla no banco (qualquer status/data) para os 6 só-Shopee, e lookup direto na API Shopee (sem filtro de janela) para os 6 só-CDS — dados necessários para apontar causa exata por order_sn. Também limitado `pedidos_valor_diferente` (973 itens — quase todos os pedidos em comum têm `buyer_paid_amount` diferente entre Shopee e CDS) a uma amostra de 20 + agregados, para não gerar payload gigante. **Achado novo, não investigado ainda:** a divergência de `buyer_paid_amount` é praticamente universal (973 de 973 pedidos em comum), o que sugere causa sistêmica (fórmula de rateio) e não um problema pontual — registrado em `BUGS.md` para tratar separadamente do problema dos 12 pedidos.
- **Fase 1 encerrada tecnicamente, com limitação documentada.** Rodado `dashboard-formulas` (create_time/update_time × ~14 grupos de status): melhor resultado foi `create_time + exceto_unpaid_e_cancelado` = 979 pedidos / R$21.977,42. Nenhuma combinação bateu com os 989 pedidos / R$22.339,82 do painel oficial Shopee. Decisão do usuário: encerrar a investigação ativa — a CDS (979) já bate em quantidade com a melhor regra reproduzível pela API pública; o gap de 10 pedidos/≈R$350 é tratado como regra de agregação interna do Seller Center não exposta pela API, não como bug do CDS. Ressalva registrada e não investigada: o "979" via API vale R$21.977,42, contra R$21.990,48 da CDS — R$13,06 de diferença nunca verificada order_sn a order_sn. Ver `BUGS.md` e `DECISIONS.md` para o registro completo. Gerado checklist de limpeza pré-Fase 2 em `ROADMAP.md` (nenhum item executado ainda).
- Rodado `reconcile-989`: universo Shopee por create_time deu `total_todos_status=1102` e `total_status_paid=975` — nenhum bate com os 989 do painel oficial (975 fica mais perto dos 979 do CDS que dos 989 da Shopee, achado novo). Reconciliação pausada por decisão do usuário: sem saber exatamente qual filtro de status o painel oficial da Shopee usa para chegar em 989, qualquer reconciliação estaria comparando conjuntos diferentes por definição, não por bug.
- Corrigido `dashboard-formulas`: `get_order_list` da Shopee **não aceita `pay_time`** como `time_range_field` — erro real da API: "must use create_time or update_time" (descoberto rodando o endpoint, 2026-07-06). Removido `pay_time` da lista de campos testados; mantidos `create_time` e `update_time`. Adicionado tratamento de falha por campo (`campos_nao_suportados` no JSON) para que uma falha em um campo não derrube os demais testes.
- Criado o endpoint `GET /api/debug/dashboard-formulas` (`app/api/debug/dashboard-formulas/route.ts`): não reconcilia banco, só testa contra a API Shopee várias combinações de campo-de-tempo (create_time/pay_time/update_time) × grupo-de-status (todos, paid, completed, shipped, completed+shipped, paid+cancelled, exceto-unpaid, cada status bruto isolado, etc.), buscando qual delas reproduz exatamente 989 pedidos / R$22.339,82. Uma única leitura da API (lista + detalhe) é reaproveitada para calcular todas as combinações em memória — não faz uma chamada por combinação.
- Criado o endpoint `GET /api/debug/reconcile-989` (`app/api/debug/reconcile-989/route.ts`), substituindo o `full-reconciliation` para fins de conclusão. Motivo: `full-reconciliation` usava união de `create_time` e `update_time` para montar o universo Shopee, e isso trouxe ~574 pedidos "só Shopee" de dias completamente distintos (14/06, 20/06, 21/06, 23/06...) — `update_time` muda toda vez que o status do pedido muda, então pedidos antigos que só mudaram de status em 02/07 entravam no universo. `reconcile-989` usa **só `create_time`** do lado Shopee (campo imutável, mesma semântica do prefixo do `order_sn`) e reproduz literalmente a regra que o próprio dashboard do CDS já usa do lado do banco (`data = '2026-07-02' AND status = 'paid'`). Cruza os dois por `order_sn`/`order_id` e retorna só as 5 categorias de diferença pedidas (só-Shopee, só-CDS, status diferente, data diferente, valor diferente), com motivo exato por pedido — sem tolerância, sem conclusão automática de causa. Ver `BUGS.md` e `API_RULES.md`.
- Criado o endpoint `GET /api/debug/full-reconciliation` (`app/api/debug/full-reconciliation/route.ts`): reconciliação pedido-a-pedido entre a API oficial da Shopee (fonte da verdade, sem filtro de status, união de listagem por `create_time` e por `update_time` para 2026-07-02) e o banco (janela ampla 2026-06-25 a 2026-07-10, match por `order_id`/`order_sn`, ignorando o campo `data` do banco por ele estar sob suspeita). Gera conjuntos A (só Shopee), B (só banco) e C (em ambos, com divergências campo a campo). Não gera conclusão automática de causa — isso é feito por leitura humana dos `order_sn` retornados, a pedido explícito do usuário ("não utilize hipóteses"). Motivo: os três endpoints de debug anteriores (`boundary-audit`, `nao-paid-02jul`, `shopee-audit`) chegaram a resultado PARCIAL/NAO_CONFIRMADO — nenhuma das duas hipóteses testadas (timezone, status) explicou os 10 pedidos / R$349,34 de diferença. Ver `BUGS.md` e `DECISIONS.md`.
- Criada a pasta `/docs` com a documentação oficial permanente do projeto (PROJECT_CONTEXT, CHANGELOG, ROADMAP, BUGS, API_RULES, DATABASE, BUSINESS_RULES, DECISIONS), a partir do código-fonte existente e dos relatórios `AUDITORIA_FINAL.md` e `RELATORIO_FASE1.md`. Nenhum código de aplicação foi alterado.
- Realizada análise (somente leitura) do endpoint `/api/debug/boundary-audit` e dos endpoints correlatos (`shopee-audit`, `nao-paid-02jul`, `pending-compare`) a pedido do usuário, para responder se a hipótese de timezone (UTC no `order_sn` vs BRT em `pedidos.data`) explica o gap de 10 pedidos / R$349,34 entre Shopee (989 / R$22.339,82) e CDS (979 / R$21.990,48). Ver relatório entregue no chat e `BUGS.md`. Não foi possível obter o resultado numérico ao vivo do endpoint — o ambiente de execução do assistente não tem acesso de rede ao Supabase.

## 2026-07-03

- Reescrita do sync Shopee (`lib/sync-shopee.ts`): income_distribution, paginação, status, upsert, item_subtotal. Resultado: recuperação de 190 pedidos (799 → 979 de 989 oficiais). Ver `AUDITORIA_FINAL.md` e `RELATORIO_FASE1.md`.
- Auditoria geral de segurança/UX/qualidade: 10 bugs corrigidos (ver `BUGS.md`).
- Migration `20260703_pedidos_novos_campos.sql`: 10 novas colunas em `pedidos`.
